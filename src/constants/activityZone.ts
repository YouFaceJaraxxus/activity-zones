import { CANVAS_RATIO, WIDTH_BASE } from "./screen";

export const ACTIVITY_ZONES_LOCAL_STORAGE_KEY = "activity-zones";

export const DEFAULT_COLOR="#ff0000" // red
export const DEFAULT_WIDTH=200;
export const DEFAULT_HEIGHT=100;
export const DEFAULT_X = WIDTH_BASE / 2 - DEFAULT_WIDTH / 2;
export const DEFAULT_Y = WIDTH_BASE / CANVAS_RATIO / 4 - DEFAULT_HEIGHT / 2;